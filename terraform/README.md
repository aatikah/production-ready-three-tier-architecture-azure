# Azure Terraform

The root Azure README contains deployment instructions. Copy `../terraform.tfvars.example` to `terraform.tfvars`, replace the placeholder values, then run Terraform from this directory.
